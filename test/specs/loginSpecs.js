const loginPage = require('../pages/login');
const Matchers = require('../helpers/customMatchers');
const isMobileResolution = require('../helpers/resolution');

describe('Perform login', () => {
it ('login using valid credentials', () => {
    loginPage.open();

})
})